# Add the real value in Vercel → Project Settings → Environment Variables.
# Never commit a real API key.
OPENAI_API_KEY=

# Optional. NutriPath uses this current vision-capable model by default.
OPENAI_MODEL=gpt-5.6
